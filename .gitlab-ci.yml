image: node:latest

stages:
  - build
  - deploy

cache:
  paths:
    - node_modules/

buildJob:
  stage: build
  script:
    - yarn install
    - yarn build
  artifacts:
    name: "$CI_PIPELINE_ID"
    paths:
      - build
    when: on_success

deployJob:
  stage: deploy
  script:
    - echo "Set deployment variables"
    - echo "Deployment scripts"
